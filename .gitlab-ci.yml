stages:
  - deploy # Run deploy after build

build-front-end-job:
  stage: deploy
  image: node:lts-bullseye
  script:
    - cd ghi
    - npm install
    - npm run build # Build the React application
    - cp -a dist/. public/
  artifacts:
    paths:
      - ghi/dist/ # Save all of the files in the build path
